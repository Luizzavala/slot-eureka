{
  "name": "eureka-slot",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
